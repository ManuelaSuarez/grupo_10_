<!DOCTYPE html>
<html lang="es">
  <%-include('../partials/head', {styles: ["login-register"]}) %>
  <body>
    <%-include('../partials/header') %>

    
    <div class="container">
      <div>
        <div class="perfil">
          <h2>Hola, <%= user.first_name%> <%= user.last_name %></h2>
          <img src="/images/users/<%= user.avatar %> " />
          <div class="email"><b><%= user.email %></b></div>
          <a class="logout" href="/users/logout">Cerrar sesion</a>
        </div>
      </div>
    </div>

    <%-include('../partials/footer') %>
  </body>
</html>
